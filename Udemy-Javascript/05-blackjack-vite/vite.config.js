import { defineConfig } from "vite";

export default defineConfig ({
    base: '/NOMBRE-DEL-REPOSITORIO/',  //Esto se usa para configurar el basepath a la hora de desplegar la página en github (GitHub Pages)
});