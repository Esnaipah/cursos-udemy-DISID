(()=>{

    /**
     * Esto declara una tupla a la que se le puede asignar un valor string
     * en la primera posición y un valor number en la segunda posición
     */
    const hero: [string, number] = ['Dr Strange', 100]

    console.log(hero);
    
})()